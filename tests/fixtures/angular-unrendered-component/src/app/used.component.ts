import { Component } from "@angular/core";

// Rendered as `<app-used>` in ShellComponent's template: credited, not flagged.
@Component({
  selector: "app-used",
  template: `<span>used</span>`,
})
export class UsedComponent {}
